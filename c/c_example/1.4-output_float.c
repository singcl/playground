#include <stdio.h>

int main() {
    float f; // 申明浮点数变量
    f = 12.3343434; // 定义浮点数变量

    double d; // 申明双精度变量
    d = 12.33232424; // 定义双精度变量

    printf("f value is: %f", f);
    printf("double d value is %e", d);
    return 0;
}
